syntax = "proto3";

package logs;

options go_package = "/logs";

message Log{
    string name = 1;
    string data = 2;
}

message Logrequest {
    Log LogEntry = 1;
}

message LogResponse {
    string result = 1;
}

services LogService {
    rpc WriteLog(Logrequest) returns (LogResponse);
}